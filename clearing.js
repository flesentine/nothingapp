(()=>{
const BS54=save,BR54=renderAll,O54=JSON.parse(localStorage.getItem('nothing-state-v54')||'null')||{},cl=(v,a,b)=>Math.max(a,Math.min(b,v)),pick=a=>a&&a.length?a[Math.floor(Math.random()*a.length)]:null;
S.clearinghouses54=O54.clearinghouses54||[];
S.clearingMembers54=O54.clearingMembers54||[];
S.clearedPositions54=O54.clearedPositions54||[];
S.marginCalls54=O54.marginCalls54||[];
S.settlementQueue54=O54.settlementQueue54||[];
S.settlementCycles54=O54.settlementCycles54||[];
S.clearinghouseSerial54=O54.clearinghouseSerial54||0;
S.clearingMemberSerial54=O54.clearingMemberSerial54||0;
S.clearedPositionSerial54=O54.clearedPositionSerial54||0;
S.marginCallSerial54=O54.marginCallSerial54||0;
S.settlementItemSerial54=O54.settlementItemSerial54||0;
S.settlementCycleSerial54=O54.settlementCycleSerial54||0;
S.novations54=O54.novations54||0;
S.marginCallsIssued54=O54.marginCallsIssued54||0;
S.marginCallsMet54=O54.marginCallsMet54||0;
S.memberDefaults54=O54.memberDefaults54||0;
S.defaultFundUses54=O54.defaultFundUses54||0;
S.memberAssessments54=O54.memberAssessments54||0;
S.settlementsFinal54=O54.settlementsFinal54||0;
S.settlementsFailed54=O54.settlementsFailed54||0;
S.emergencyFinality54=O54.emergencyFinality54||0;
S.clearinghouseFailures54=O54.clearinghouseFailures54||0;
S.nettingBreaks54=O54.nettingBreaks54||0;
S.clearingCrises54=O54.clearingCrises54||0;
S.lastClearingCrisis54=O54.lastClearingCrisis54||0;
S.clearingKeys54=O54.clearingKeys54||{};
S.lastClearingTick54=O54.lastClearingTick54||Date.now();
function save54(){BS54();localStorage.setItem('nothing-state-v54',JSON.stringify({clearinghouses54:S.clearinghouses54,clearingMembers54:S.clearingMembers54,clearedPositions54:S.clearedPositions54,marginCalls54:S.marginCalls54,settlementQueue54:S.settlementQueue54,settlementCycles54:S.settlementCycles54,clearinghouseSerial54:S.clearinghouseSerial54,clearingMemberSerial54:S.clearingMemberSerial54,clearedPositionSerial54:S.clearedPositionSerial54,marginCallSerial54:S.marginCallSerial54,settlementItemSerial54:S.settlementItemSerial54,settlementCycleSerial54:S.settlementCycleSerial54,novations54:S.novations54,marginCallsIssued54:S.marginCallsIssued54,marginCallsMet54:S.marginCallsMet54,memberDefaults54:S.memberDefaults54,defaultFundUses54:S.defaultFundUses54,memberAssessments54:S.memberAssessments54,settlementsFinal54:S.settlementsFinal54,settlementsFailed54:S.settlementsFailed54,emergencyFinality54:S.emergencyFinality54,clearinghouseFailures54:S.clearinghouseFailures54,nettingBreaks54:S.nettingBreaks54,clearingCrises54:S.clearingCrises54,lastClearingCrisis54:S.lastClearingCrisis54,clearingKeys54:S.clearingKeys54,lastClearingTick54:S.lastClearingTick54}))}save=save54;
const realm=id=>S.real[id],dealer=id=>(S.derivativeDealers53||[]).find(d=>d.id===id),swap=id=>(S.creditSwaps53||[]).find(s=>s.id===id),bond=id=>(S.softwareBonds52||[]).find(b=>b.id===id),ccp=id=>S.clearinghouses54.find(c=>c.id===id),member=id=>S.clearingMembers54.find(m=>m.id===id);
function trust54(id,status,note){let accepted=S.trustPolicies?.[id]?.acceptedRoots||[],r=[...(S.trustRoots||[])].filter(x=>accepted.includes(x.id)&&x.status==='active').sort((a,b)=>(b.strength||0)-(a.strength||0))[0];S.trustAudits=S.trustAudits||[];S.trustAuditSerial=S.trustAuditSerial||0;S.trustAudits.push({id:'TA'+(++S.trustAuditSerial),reality:id,remediationId:null,credentialId:null,certificateId:null,rootId:r?.id||null,status,created:Date.now(),note,x:10+Math.random()*80,y:12+Math.random()*68})}
function incident54(id,title,severity='SEV1'){S.incidents=S.incidents||[];let ex=S.incidents.find(i=>i.reality===id&&i.title===title&&i.status==='open');if(ex)return ex;S.incidentSerial=S.incidentSerial||0;let i={id:'INC'+(++S.incidentSerial),reality:id,title,severity,status:'open',alertIds:[],started:Date.now(),acknowledged:null,resolved:null,postmortem:null,owner:'software clearing house',x:10+Math.random()*80,y:12+Math.random()*68};S.incidents.push(i);return i}
function ensureClearinghouses(){for(const id of ['A','B']){let c=S.clearinghouses54.find(c=>c.reality===id);if(!c){c={id:'CCP'+(++S.clearinghouseSerial54),reality:id,name:'Reality '+id+' Software Clearing House',status:'operating',capital:16,defaultFund:24,liquidity:12,guaranteeFund:8,settlementFinality:'ordinary',cycles:0,defaults:0,assessments:0,nettingBenefit:0,created:Date.now(),x:44+Math.random()*12,y:16+Math.random()*18};S.clearinghouses54.push(c);remember('CLEARING HOUSE '+c.id+': Reality '+id+' centralized settlement of software-credit derivatives',realm(id));trust54(id,'software-clearinghouse-created',c.id+' '+c.name)}for(const d of (S.derivativeDealers53||[]).filter(d=>d.reality===id)){if(!S.clearingMembers54.some(m=>m.ccpId===c.id&&m.dealerId===d.id)){let posted=Math.min(4,Math.max(1,(d.collateral||0)*.22));if(d.collateral!=null)d.collateral=Math.max(0,d.collateral-posted);S.clearingMembers54.push({id:'CM'+(++S.clearingMemberSerial54),ccpId:c.id,reality:id,dealerId:d.id,dealer:d.name,status:d.status==='solvent'?'active':'watch',initialMargin:posted,variationMargin:0,assessmentPaid:0,defaults:0,netExposure:0,grossExposure:0,joined:Date.now(),x:10+Math.random()*80,y:12+Math.random()*68})}}}}
function house(id){ensureClearinghouses();return S.clearinghouses54.find(c=>c.reality===id)}
function dealerMember(did){return S.clearingMembers54.find(m=>m.dealerId===did&&['active','watch','defaulted'].includes(m.status))}
function gradeRisk54(g){let map={AAA:.03,AA:.05,A:.08,BBB:.12,BB:.2,B:.3,CCC:.48,D:.85};return map[g]??.15}
function bondRisk54(b){if(!b)return .2;if(b.status==='default')return 1;let r=(S.creditRatings52||[]).filter(r=>r.subjectType===b.subjectType&&r.subjectId===b.subjectId).sort((a,b)=>b.published-a.published)[0];return cl(gradeRisk54(r?.grade||'BBB')+(b.status==='active'?0:.1),.03,1)}
function clearedForSwap(sid){return S.clearedPositions54.find(p=>p.swapId===sid&&p.status!=='terminated')}
function novateSwap(s,manual=true){if(!s||!['active','settled','failed','counterparty-default'].includes(s.status))return null;ensureClearinghouses();if(clearedForSwap(s.id))return clearedForSwap(s.id);let c=house(s.reality),m=dealerMember(s.sellerId);if(!c||!m)return null;let gross=s.notional||0,risk=bondRisk54(bond(s.bondId)),im=Math.max(.5,gross*(.05+risk*.08));let p={id:'CP'+(++S.clearedPositionSerial54),ccpId:c.id,reality:s.reality,swapId:s.id,bondId:s.bondId,memberId:m.id,dealerId:s.sellerId,status:s.status==='active'?'open':'event-observed',notional:gross,initialMargin:im,variationMargin:0,nettedMargin:im,creditEventObserved:false,settledAmount:0,created:Date.now(),x:10+Math.random()*80,y:12+Math.random()*68};S.clearedPositions54.push(p);s.cleared54=true;s.ccp54=c.id;S.novations54++;remember('NOVATION '+p.id+': '+s.id+' moved between '+m.dealer+' and '+c.name,realm(s.reality));trust54(s.reality,'software-derivative-novated',p.id+' '+s.id+' '+c.id);recomputeNetting(c);if(manual){$('#msg').textContent=s.id+' novated to '+c.name+'.';renderAll();save54();openPosition(p)}return p}
function clearBook(id=S.active,manual=true){ensureClearinghouses();let xs=(S.creditSwaps53||[]).filter(s=>s.reality===id&&['active','settled','failed','counterparty-default'].includes(s.status)&&!clearedForSwap(s.id));for(const s of xs)novateSwap(s,false);if(manual){$('#msg').textContent=xs.length+' derivative positions novated into central clearing.';renderAll();save54();openHouse(house(id))}return xs.length}
function recomputeNetting(c){let ms=S.clearingMembers54.filter(m=>m.ccpId===c.id&&m.status!=='defaulted');let grossAll=0,netAll=0;for(const m of ms){let ps=S.clearedPositions54.filter(p=>p.ccpId===c.id&&p.memberId===m.id&&p.status==='open'),byBond={};for(const p of ps){let r=bondRisk54(bond(p.bondId));byBond[p.bondId]=(byBond[p.bondId]||0)+p.notional*r;grossAll+=p.notional*r}let gross=ps.reduce((n,p)=>n+p.notional*bondRisk54(bond(p.bondId)),0),net=Object.values(byBond).reduce((n,x)=>n+Math.abs(x),0);m.grossExposure=gross;m.netExposure=net;netAll+=net;for(const p of ps){let share=gross?net/gross:1;p.nettedMargin=Math.max(.35,p.initialMargin*cl(share,.25,1))}}c.nettingBenefit=Math.max(0,grossAll-netAll)}
function requiredVariation(p){let b=bond(p.bondId),risk=bondRisk54(b),base=p.notional*risk*.42;if(b?.status==='default')base=Math.max(base,p.notional*.72);return Math.max(0,base-p.initialMargin)}
function openMarginFor(p){return S.marginCalls54.find(x=>x.positionId===p.id&&x.status==='open')}
function issueMargin(p,manual=false){if(!p||p.status!=='open'||openMarginFor(p))return null;let req=requiredVariation(p),due=Math.max(0,req-(p.variationMargin||0));if(due<.5)return null;let x={id:'MC'+(++S.marginCallSerial54),ccpId:p.ccpId,reality:p.reality,memberId:p.memberId,positionId:p.id,dealerId:p.dealerId,amount:Math.round(due*10)/10,status:'open',reason:bond(p.bondId)?.status==='default'?'underlying bond default':'mark-to-market deterioration',issued:Date.now(),due:Date.now()+18000,paid:0,x:10+Math.random()*80,y:12+Math.random()*68};S.marginCalls54.push(x);S.marginCallsIssued54++;remember('VARIATION MARGIN '+x.id+': '+x.amount+' credits called on '+(dealer(p.dealerId)?.name||p.dealerId)+' for '+p.swapId,realm(p.reality));if(typeof place==='function')place('cold','margin call arriving before software settlement',realm(p.reality));if(manual){renderAll();save54();openMargin(x)}return x}
function payMargin(x,manual=true){if(!x||x.status!=='open')return false;let d=dealer(x.dealerId),m=member(x.memberId),p=S.clearedPositions54.find(p=>p.id===x.positionId),c=ccp(x.ccpId);if(!d||!m||!p||!c)return false;let available=Math.max(0,(d.capital||0)+(d.collateral||0));let pay=Math.min(x.amount,available);let fromCapital=Math.min(d.capital||0,pay);d.capital=Math.max(0,(d.capital||0)-fromCapital);let rest=pay-fromCapital;if(rest>0)d.collateral=Math.max(0,(d.collateral||0)-rest);x.paid=pay;m.variationMargin+=pay;p.variationMargin+=pay;c.liquidity+=pay;if(pay+1e-9>=x.amount){x.status='met';x.met=Date.now();S.marginCallsMet54++;remember('MARGIN MET '+x.id+': '+pay.toFixed(1)+' credits posted to '+c.id,realm(x.reality));if(manual)$('#msg').textContent=x.id+' met.';return true}x.status='failed';x.failed=Date.now();remember('MARGIN FAILURE '+x.id+': member posted '+pay.toFixed(1)+' of '+x.amount.toFixed(1),realm(x.reality));defaultMember(m,'margin failure '+x.id);if(manual)$('#msg').textContent=x.id+' failed. Clearing member default waterfall started.';return false}
function enqueueSettlement(p){if(!p||S.settlementQueue54.some(q=>q.positionId===p.id))return null;let s=swap(p.swapId);if(!s||!['settled','failed','counterparty-default'].includes(s.status))return null;let amount=Math.max(0,s.payout??s.notional??0),q={id:'SET'+(++S.settlementItemSerial54),ccpId:p.ccpId,reality:p.reality,positionId:p.id,swapId:p.swapId,memberId:p.memberId,dealerId:p.dealerId,amount,status:'queued',sourceStatus:s.status,created:Date.now(),settled:0,final:false,x:10+Math.random()*80,y:12+Math.random()*68};S.settlementQueue54.push(q);p.creditEventObserved=true;p.status='settlement-pending';remember('SETTLEMENT QUEUED '+q.id+': '+p.swapId+' requires '+amount.toFixed(1)+' credits through '+p.ccpId,realm(p.reality));return q}
function settlementCycle(c,emergency=false,manual=true){if(!c||!['operating','stressed','frozen'].includes(c.status))return null;let items=S.settlementQueue54.filter(q=>q.ccpId===c.id&&q.status==='queued'),cy={id:'SC'+(++S.settlementCycleSerial54),ccpId:c.id,reality:c.reality,status:'processing',itemIds:items.map(x=>x.id),gross:items.reduce((n,x)=>n+x.amount,0),settled:0,shortfall:0,emergency,created:Date.now(),closed:null,x:10+Math.random()*80,y:12+Math.random()*68};S.settlementCycles54.push(cy);c.cycles++;for(const q of items){let p=S.clearedPositions54.find(p=>p.id===q.positionId),m=member(q.memberId),need=q.amount;let paid=Math.min(need,(m?.variationMargin||0)+(m?.initialMargin||0));if(m){let vm=Math.min(m.variationMargin||0,paid);m.variationMargin-=vm;let rest=paid-vm;m.initialMargin=Math.max(0,(m.initialMargin||0)-rest)}need-=paid;if(need>0){let use=Math.min(need,c.defaultFund);c.defaultFund-=use;need-=use;paid+=use;if(use>0){S.defaultFundUses54++;remember('DEFAULT FUND USE: '+c.id+' spent '+use.toFixed(1)+' on '+q.id,realm(c.reality))}}if(need>0){let surv=S.clearingMembers54.filter(x=>x.ccpId===c.id&&x.status==='active'&&x.id!==q.memberId),assess=surv.length?need/surv.length:0;for(const sm of surv){let d=dealer(sm.dealerId),take=Math.min(assess,d?.capital||0);if(d)d.capital-=take;c.liquidity+=take;sm.assessmentPaid+=take;S.memberAssessments54+=take;need-=take}paid=q.amount-Math.max(0,need)}q.settled=Math.max(0,paid);cy.settled+=q.settled;if(need<=.01){q.status='settled-provisional';p.status='settled-provisional'}else{q.status='shortfall';cy.shortfall+=need;S.settlementsFailed54++;if(m&&m.status!=='defaulted')defaultMember(m,'settlement shortfall '+q.id)}}if(cy.shortfall<=.01){cy.status='final';cy.closed=Date.now();for(const q of items){q.status='final';q.final=true;let p=S.clearedPositions54.find(p=>p.id===q.positionId);if(p)p.status='final'}S.settlementsFinal54+=items.length;remember('SETTLEMENT FINALITY '+cy.id+': '+items.length+' cleared obligations became final',realm(c.reality));trust54(c.reality,'software-settlement-final',cy.id+' '+items.length+' obligations')}else if(emergency){cy.status='emergency-final';cy.closed=Date.now();S.emergencyFinality54++;for(const q of items){q.final=true;if(q.status==='settled-provisional')q.status='final';else q.status='final-with-shortfall'}c.settlementFinality='emergency';remember('EMERGENCY SETTLEMENT FINALITY '+cy.id+': '+cy.shortfall.toFixed(1)+' credits remain unpaid under a final cycle',realm(c.reality));if(typeof place==='function'){place('cold','final settlement containing an unpaid obligation',realm(c.reality));place('dream','payment finality declared over a shortfall',realm(c.reality))}clearingCrisis(c.reality,'emergency-finality')}else{cy.status='failed';cy.closed=Date.now();c.status='frozen';remember('SETTLEMENT CYCLE FAILED '+cy.id+': '+cy.shortfall.toFixed(1)+' credits could not settle',realm(c.reality));if(typeof place==='function')place('cold','clearinghouse frozen with valid trades waiting inside',realm(c.reality));failHouse(c,'settlement shortfall')}if(manual){$('#msg').textContent=cy.id+' '+cy.status.toUpperCase()+' · shortfall '+cy.shortfall.toFixed(1)+'.';renderAll();save54();openCycle(cy)}return cy}
function defaultMember(m,reason){if(!m||m.status==='defaulted')return;m.status='defaulted';m.defaults++;S.memberDefaults54++;let d=dealer(m.dealerId);if(d&&d.status==='solvent')d.status='default';let c=ccp(m.ccpId);if(c){c.defaults++;c.status=c.status==='operating'?'stressed':c.status}remember('CLEARING MEMBER DEFAULT '+m.id+': '+m.dealer+' defaulted after '+reason,realm(m.reality));if(typeof place==='function')place('cold','member default entering the clearinghouse waterfall',realm(m.reality));breakNetting(m);incident54(m.reality,'clearing member default: '+m.dealer,'SEV2')}
function breakNetting(m){let ps=S.clearedPositions54.filter(p=>p.memberId===m.id&&['open','settlement-pending'].includes(p.status));if(!ps.length)return;S.nettingBreaks54++;for(const p of ps)p.nettedMargin=p.initialMargin;remember('CLOSE-OUT NETTING BROKE: '+m.dealer+' gross obligations reappeared after member default',realm(m.reality));if(typeof place==='function')place('dream','net exposure becoming gross exposure at default',realm(m.reality));clearingCrisis(m.reality,'netting-break')}
function failHouse(c,why){if(!c||c.status==='failed')return;c.status='failed';S.clearinghouseFailures54++;for(const q of S.settlementQueue54.filter(q=>q.ccpId===c.id&&!q.final&&['queued','shortfall','settled-provisional'].includes(q.status)))q.status='frozen';for(const p of S.clearedPositions54.filter(p=>p.ccpId===c.id&&['open','settlement-pending','settled-provisional'].includes(p.status)))p.status='frozen';remember('CLEARING HOUSE FAILURE '+c.id+': '+why+' froze centrally cleared software-credit obligations',realm(c.reality));if(typeof place==='function'){place('cold','institution built to remove counterparty risk became the counterparty',realm(c.reality));place('dream','too central to settle and too connected to fail',realm(c.reality))}incident54(c.reality,'software clearing house failed: '+c.id,'SEV1');clearingCrisis(c.reality,'ccp-failure')}
function recapitalize(c){if(!c||!['failed','frozen','stressed'].includes(c.status))return;let add=18;c.capital+=add;c.liquidity+=add*.7;c.defaultFund+=add*.3;c.status='operating';remember('CLEARING LIQUIDITY BRIDGE: Reality '+c.reality+' supplied '+add+' credits to reopen '+c.id,realm(c.reality));if(typeof place==='function')place('warm','public liquidity bridge beneath private settlement',realm(c.reality));trust54(c.reality,'software-clearing-liquidity-bridge',c.id+' '+add+' credits');renderAll();save54();openHouse(c)}
function observeDerivativeEvents(){ensureClearinghouses();for(const s of (S.creditSwaps53||[])){let p=clearedForSwap(s.id);if(!p&&['active','settled','failed','counterparty-default'].includes(s.status))p=novateSwap(s,false);if(!p)continue;if(p.status==='open')issueMargin(p,false);if(['settled','failed','counterparty-default'].includes(s.status)&&!S.settlementQueue54.some(q=>q.positionId===p.id))enqueueSettlement(p)}}
function clearingCrisis(id,kind){let now=Date.now();if(now-S.lastClearingCrisis54<5000)return;S.lastClearingCrisis54=now;S.clearingCrises54++;remember('CLEARING CRISIS: '+kind+' made settlement infrastructure itself a source of systemic authority',realm(id));if(typeof place==='function')place('cold','settlement finality depending on one institution staying solvent',realm(id));let b=$('#bailiff');if(b)b.textContent='asking whether a trade exists before or after the clearinghouse says it settled'}
function stressHouse(id=S.active){let c=house(id),ms=S.clearingMembers54.filter(m=>m.ccpId===c.id&&m.status==='active');if(!ms.length){$('#msg').textContent='No active clearing members exist.';return}let m=ms.sort((a,b)=>b.grossExposure-a.grossExposure)[0],ps=S.clearedPositions54.filter(p=>p.memberId===m.id&&p.status==='open');if(ps.length){let p=ps.sort((a,b)=>b.notional-a.notional)[0],x=issueMargin(p,false);if(x){x.amount=Math.max(x.amount,(dealer(m.dealerId)?.capital||0)+(dealer(m.dealerId)?.collateral||0)+2);payMargin(x,false)}else defaultMember(m,'clearing stress test')}else defaultMember(m,'clearing stress test');renderAll();save54();openHouse(c)}
function openPosition(p){let s=swap(p.swapId),m=member(p.memberId),b=[['call variation margin',()=>{let x=issueMargin(p,true);if(!x)$('#msg').textContent='No additional variation margin is due.'}],['close',closePanel]];panel(p.id+' · CLEARED '+p.swapId,p.status+' · '+p.ccpId,'member '+(m?.dealer||p.dealerId)+' · notional '+p.notional.toFixed(1)+' · initial '+p.initialMargin.toFixed(1)+' · variation '+p.variationMargin.toFixed(1)+' · netted margin '+p.nettedMargin.toFixed(1)+' · bond '+p.bondId+' '+(bond(p.bondId)?.status||'unknown')+(s?.naked?' · naked CDS':''),b)}
function openMargin(x){let d=dealer(x.dealerId),b=[];if(x.status==='open')b.push(['meet margin call',()=>{payMargin(x,true);renderAll();save54();openMargin(x)}]);b.push(['close',closePanel]);panel(x.id+' · VARIATION MARGIN',x.status+' · '+x.reason,(d?.name||x.dealerId)+' owes '+x.amount.toFixed(1)+' · paid '+x.paid.toFixed(1)+' · due '+new Date(x.due).toLocaleTimeString(),b)}
function emergencyFinalize(cy,manual=true){if(!cy||(!['failed','processing'].includes(cy.status)&&cy.shortfall<=0))return false;cy.status='emergency-final';cy.emergency=true;cy.closed=Date.now();S.emergencyFinality54++;for(const id of cy.itemIds){let q=S.settlementQueue54.find(q=>q.id===id);if(!q)continue;q.final=true;q.status=(q.settled+0.01>=q.amount)?'final':'final-with-shortfall';let p=S.clearedPositions54.find(p=>p.id===q.positionId);if(p)p.status=q.status}let c=ccp(cy.ccpId);if(c)c.settlementFinality='emergency';remember('EMERGENCY SETTLEMENT FINALITY '+cy.id+': '+cy.shortfall.toFixed(1)+' credits remain unpaid under a final cycle',realm(cy.reality));if(typeof place==='function'){place('cold','final settlement containing an unpaid obligation',realm(cy.reality));place('dream','payment finality declared over a shortfall',realm(cy.reality))}clearingCrisis(cy.reality,'emergency-finality');if(manual){$('#msg').textContent=cy.id+' declared FINAL despite '+cy.shortfall.toFixed(1)+' unpaid.';renderAll();save54();openCycle(cy)}return true}
function openCycle(cy){let b=[];if(cy.status==='failed'||(cy.shortfall>0&&!cy.emergency))b.push(['declare emergency finality',()=>emergencyFinalize(cy,true)]);b.push(['close',closePanel]);panel(cy.id+' · SETTLEMENT CYCLE',cy.status+' · '+cy.itemIds.length+' obligations','gross '+cy.gross.toFixed(1)+' · settled '+cy.settled.toFixed(1)+' · shortfall '+cy.shortfall.toFixed(1)+(cy.emergency?' · EMERGENCY FINALITY':''),b)}
function openMember(m){let d=dealer(m.dealerId),ps=S.clearedPositions54.filter(p=>p.memberId===m.id),calls=S.marginCalls54.filter(x=>x.memberId===m.id),b=ps.slice(-4).map(p=>['open '+p.id,()=>openPosition(p)]);for(const x of calls.filter(x=>x.status==='open').slice(-2))b.push(['open '+x.id,()=>openMargin(x)]);b.push(['close',closePanel]);panel(m.dealer+' · CLEARING MEMBER',m.status+' · '+m.id,'dealer capital '+(d?.capital||0).toFixed(1)+' · initial margin '+m.initialMargin.toFixed(1)+' · variation '+m.variationMargin.toFixed(1)+' · gross '+m.grossExposure.toFixed(1)+' · net '+m.netExposure.toFixed(1)+' · assessments '+m.assessmentPaid.toFixed(1),b)}
function openHouse(c){recomputeNetting(c);let ms=S.clearingMembers54.filter(m=>m.ccpId===c.id),queued=S.settlementQueue54.filter(q=>q.ccpId===c.id&&['queued','shortfall','settled-provisional','frozen'].includes(q.status)),b=[['clear derivative book',()=>clearBook(c.reality)],['run margin cycle',()=>{for(const p of S.clearedPositions54.filter(p=>p.ccpId===c.id&&p.status==='open')){let x=issueMargin(p,false);if(x)payMargin(x,false)}renderAll();save54();openHouse(c)}],['settle queued obligations',()=>settlementCycle(c,false,true)],['stress clearing member',()=>stressHouse(c.reality)]];if(['failed','frozen','stressed'].includes(c.status))b.push(['public liquidity bridge',()=>recapitalize(c)]);let failedCycle=[...S.settlementCycles54].reverse().find(x=>x.ccpId===c.id&&x.status==='failed');if(failedCycle)b.push(['declare emergency finality',()=>emergencyFinalize(failedCycle,true)]);for(const m of ms.slice(0,6))b.push([m.dealer+' · '+m.status,()=>openMember(m)]);for(const cy of S.settlementCycles54.filter(x=>x.ccpId===c.id).slice(-3))b.push(['open '+cy.id,()=>openCycle(cy)]);b.push(['close',closePanel]);panel(c.name+' · '+c.status.toUpperCase(),c.id+' · central counterparty','capital '+c.capital.toFixed(1)+' · default fund '+c.defaultFund.toFixed(1)+' · liquidity '+c.liquidity.toFixed(1)+' · '+ms.length+' members · '+queued.length+' unsettled · netting benefit '+c.nettingBenefit.toFixed(1)+' · defaults '+c.defaults,b)}
function openClearing(id=S.active){openHouse(house(id))}
function autonomous(){ensureClearinghouses();let now=Date.now();if(now-S.lastClearingTick54<1900)return;S.lastClearingTick54=now;observeDerivativeEvents();for(const x of S.marginCalls54.filter(x=>x.status==='open'&&x.due<=now)){payMargin(x,false);break}for(const c of S.clearinghouses54){let q=S.settlementQueue54.filter(x=>x.ccpId===c.id&&x.status==='queued');if(q.length&&Math.random()<.08){settlementCycle(c,false,false);break}recomputeNetting(c)}if(Math.random()<.0012)stressHouse(pick(['A','B']))}
function renderClearing(){ensureClearinghouses();let layer=$('#clearingLayer');if(!layer){layer=document.createElement('div');layer.id='clearingLayer';document.body.appendChild(layer)}layer.innerHTML='';for(const c of S.clearinghouses54){let e=document.createElement('i');e.className='clearingHouse '+c.status;e.dataset.label=c.id;e.style.left=c.x+'%';e.style.top=c.y+'%';e.onclick=()=>openHouse(c);layer.appendChild(e)}for(const m of S.clearingMembers54.slice(-12)){let e=document.createElement('i');e.className='clearingMember '+m.status;e.dataset.label='M';e.style.left=m.x+'%';e.style.top=m.y+'%';e.onclick=()=>openMember(m);layer.appendChild(e)}for(const x of S.marginCalls54.filter(x=>['open','failed'].includes(x.status)).slice(-10)){let e=document.createElement('i');e.className='marginCall '+x.status;e.dataset.label='M!';e.style.left=x.x+'%';e.style.top=x.y+'%';e.onclick=()=>openMargin(x);layer.appendChild(e)}for(const q of S.settlementQueue54.filter(q=>!q.final&&q.status!=='final').slice(-10)){let e=document.createElement('i');e.className='settlementItem '+q.status;e.dataset.label='S';e.style.left=q.x+'%';e.style.top=q.y+'%';e.onclick=()=>{let p=S.clearedPositions54.find(p=>p.id===q.positionId);if(p)openPosition(p)};layer.appendChild(e)}let line=$('#clearingline');if(!line){line=document.createElement('div');line.id='clearingline';line.className='line';($('#derivativesline')||$('#capitalline')||$('#insuranceline')||$('#enforcement')).after(line)}let id=S.active,c=house(id),queued=S.settlementQueue54.filter(q=>q.reality===id&&!q.final&&q.status!=='final').length,open=S.marginCalls54.filter(x=>x.reality===id&&x.status==='open').length;line.textContent='software clearing: '+c.status+' · '+S.clearedPositions54.filter(p=>p.reality===id).length+' cleared · '+open+' margin calls · '+queued+' unsettled · fund '+c.defaultFund.toFixed(1)+' · '+S.memberDefaults54+' member defaults · '+S.clearingCrises54+' crises';let ctr=document.querySelector('#app .controls');if(ctr&&!$('#openClearingButton')){let x=document.createElement('button');x.id='openClearingButton';x.textContent='open clearing house';x.onclick=()=>openClearing(S.active);ctr.prepend(x)}if(ctr&&!$('#clearDerivativeBookButton')){let x=document.createElement('button');x.id='clearDerivativeBookButton';x.textContent='clear derivative book';x.onclick=()=>clearBook(S.active);ctr.prepend(x)}}
renderAll=function(){BR54();renderClearing()};let f=$('#forget');if(f)f.onclick=()=>{for(let i=2;i<=54;i++)localStorage.removeItem('nothing-state-v'+i);location.reload()};
setInterval(()=>{autonomous();renderClearing();save54()},2200);ensureClearinghouses();observeDerivativeEvents();renderAll();save54();
})();